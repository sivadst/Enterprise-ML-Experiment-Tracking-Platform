# Deployment Guide

1. Ensure Docker and docker-compose are installed on your host.
2. Clone the repository.
3. Verify that ports `8000` and `3000` are free on the host machine.
4. Run `docker-compose up -d --build`.
5. Access the application at port 3000.
